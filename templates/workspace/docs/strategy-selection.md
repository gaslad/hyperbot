# Strategy Selection

This workspace was generated from Hyperbot strategy packs.

Installed packs should start as baseline defaults.
If a symbol needs custom tuning, run a token-specific review over the most relevant recent window, for example 90 days.
